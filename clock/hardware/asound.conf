# bpi-zero-clock playback-only native hardware routing
# No capture PCM, plug, rate conversion, dmix or dsnoop is introduced.

pcm.!default {
    type hw
    card MAX98357A
    device 0
}

ctl.!default {
    type hw
    card MAX98357A
}
