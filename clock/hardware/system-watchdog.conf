[Manager]
# Pet the Allwinner hardware watchdog from PID 1. If systemd/the kernel stalls
# long enough that PID 1 cannot service it, the board resets itself.
RuntimeWatchdogSec=30s
# Also protect a reboot/shutdown that gets stuck indefinitely.
RebootWatchdogSec=2min
